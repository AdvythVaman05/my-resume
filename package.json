{
  "name": "latex-resume",
  "version": "1.0.0",
  "scripts": {
    "build": "apt-get update && apt-get install -y texlive-latex-base texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra && pdflatex resume.tex",
    "start": "npx servor . index.html --port $PORT"
  },
  "dependencies": {
    "servor": "^4.0.0"
  }
}
